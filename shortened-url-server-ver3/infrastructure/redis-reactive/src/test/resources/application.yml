spring:
  profiles:
    active: test

  data:
    redis:
      standalone:
        nodes:
          server-1: localhost:6379,
          server-2: localhost:6380,
          server-3: localhost:6381,

logging:
  level:
    io.lettuce.core: debug
