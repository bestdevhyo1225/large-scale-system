spring:
  profiles:
    active: test

  post:
    redis:
      enable: true
      mode: standalone
      nodes: localhost:6379

  feed:
    redis:
      enable: true
      mode: standalone
      nodes: localhost:6379
