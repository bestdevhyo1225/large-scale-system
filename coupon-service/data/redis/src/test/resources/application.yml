spring:
  profiles:
    active: test

data:
  enable:
    redis:
      coupon: true

  redis:
    coupon:
      mode: standalone
      nodes: localhost:6379

logging:
  level:
    io.lettuce.core.protocol.CommandHandler: debug
