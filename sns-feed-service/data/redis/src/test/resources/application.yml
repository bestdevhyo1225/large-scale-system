spring:
  profiles:
    active: test

  data:
    redis:
      post:
        mode: standalone
        nodes:
          node1: localhost:6379
      feed:
        mode: standalone
        nodes:
          node1: localhost:6380

logging:
  level:
    io.lettuce.core: debug
